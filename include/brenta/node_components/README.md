# Node components

In this directory you can find all component for nodes, which are
classes that extend the `NodeComponent` class and implement its
interface. These components can be added to a node through the `Scene`
API to provide specialized functionalities.
