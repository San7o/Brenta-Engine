- move shader template implementation to a separate file
- introduce the transform class (maybe change translation?)
- introduce the viewport class
- copy the particle and text shaders to C strings before building?