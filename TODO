- make a struct that contains shader variables and their values, to
  handle cases like setting uniforms for rendering the particles
- create model from raw vertexes
- add Scene class
- rewrite logger
- fix the model / mesh texture enums mess